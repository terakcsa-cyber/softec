# Next.js (App Router / RSC) registers internal webpack loaders such as
# `next-flight-client-entry-loader`. pnpm’s default isolated `node_modules`
# layout can prevent webpack from resolving those loaders from `apps/web`.
# Hoisted linker matches npm-style layout and fixes this class of errors.
node-linker=hoisted
